barplot_function <- function(lineage_name,
                             lineage_mat,
                             membership_vec
                             umap_mat,
                             col_indicator = rgb(1, 0.1, 0.1, 0.2),
                             col_palette_enrichment = grDevices::colorRampPalette(c('lightgrey', 'blue'))(100),
                             mode = c("count", "enrichment")){
  
}