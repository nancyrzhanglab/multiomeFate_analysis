peak_mixture_modeling <- function(bin_cutoffs,
                                  mat, # rows = cells, columns = basepairs
                                  peak_locations,
                                  peak_prob,
                                  max_iter = 100,
                                  tol = 1e-6){
  
}